({
    onRender: function(component, event, helper) {
        /*window.addEventListener('mouseup', $A.getCallback(function(){
            if(component.isValid()) {
                console.log('Resizing Window');
                helper.setWidth(component,event);
            }
       }));*/
	}
})